/** The functions table.pack and table.unpack are exposed on the global namespace too, to improve compatibility with code targeting Lua 5.1. */
declare const unpack: typeof table.unpack;
/** The functions table.pack and table.unpack are exposed on the global namespace too, to improve compatibility with code targeting Lua 5.1. */
declare const pack: typeof table.pack;
